# <%= projectName %>

<%= description %>

Generated by **Sprygen** - The Spring Boot Project Generator.

## Requirements
* Java <%= javaVersion %>
<%_ if (buildTool === 'maven') { _%>
* Maven 3.8+
<%_ } else { _%>
* Gradle 8.x
<%_ } _%>
<%_ if (database === 'mysql') { _%>
* MySQL 8.0+
<%_ } else if (database === 'postgresql') { _%>
* PostgreSQL 14+
<%_ } _%>

## Getting Started

1. Configure your database settings in `src/main/resources/application.yml`
2. Run the application:
<%_ if (buildTool === 'maven') { _%>
   ```bash
   ./mvnw spring-boot:run
   ```
<%_ } else { _%>
   ```bash
   ./gradlew bootRun
   ```
<%_ } _%>

## Project Structure
- `config`: Configuration classes (Security, CORS, Swagger)
- `controller`: REST API endpoints
- `dto`: Data Transfer Objects
- `entity`: JPA Entities
- `repository`: Spring Data JPA Repositories
- `security`: JWT Security filters and services
- `service`: Business logic

## Important Endpoints

- API Base Path: `/api/v1`
<%_ if (hasSwagger) { _%>
- Swagger UI: `http://localhost:8080/swagger-ui.html`
- OpenAPI JSON: `http://localhost:8080/v3/api-docs`
<%_ } _%>
- Login: `POST /api/v1/auth/login`
- Register: `POST /api/v1/auth/register`
